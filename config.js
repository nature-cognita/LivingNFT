const config = {
    wsEndpoint: 'wss://testnet2.unique.network',
    ownerSeed: 'document acquire cloth curious drip use spell idle laptop submit peanut isolate',

    outputSchema: "schema.json",

    collectionId: 304
}

module.exports = config;